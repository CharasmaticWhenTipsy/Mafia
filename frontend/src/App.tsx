import { Input } from "@mui/material";

function App() {
  // we have working react with vite now.
  // return main login screen
  // enter pin to get started or create lobby
  // on success code return new lobby
  // else game does not exist

  return (
    <>
      <p>Enter Code to Begin</p>
      <Input></Input>
    </>
  );
}

export default App;
